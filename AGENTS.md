# AGENTS.md

本文件适用于整个仓库，供在此项目中工作的自动化代理和贡献者参考。

## 项目定位

这是一个无构建步骤、无后端依赖的移动端中文 H5 原型。保持实现轻量，除非任务明确要求，否则不要引入框架、包管理器、服务端或第三方运行时依赖。

回答系统目前是 `app.js` 中的本地关键词匹配，不是真实 AI 服务。不要在文案或实现中把模拟能力描述成已接入的生产能力。

## 代码地图

- `index.html`：首页、问答区、底部输入栏和个人中心抽屉。
- `profile.html`：八字档案新增与编辑表单。
- `chat.html`：旧入口兼容页，只负责跳转到 `index.html`。
- `styles.css`：所有页面样式。首页和档案页共享该文件。
- `shared.js`：共享状态层、`localStorage` 读写、V1 到 V2 迁移和 `window.GuoxueApp` API。
- `app.js`：首页渲染、额度、会话、模拟回答、抽屉及手势交互。
- `profile.js`：档案表单填充、日期校验、保存和删除。
- `assets/`：运行时图片资源。`candidates/` 和 `teacher-candidates/` 还包含候选素材。
- `.github/workflows/pages.yml`：GitHub Pages 发布文件清单。
- `.openai/hosting.json`：已有 OpenAI Sites 项目标识；不得另造或替换 `project_id`。

## 修改原则

1. 保持源文件为 UTF-8，保留简体中文界面和现有国风视觉语言。
2. 保持原生 HTML/CSS/JavaScript；只有明确需求和充分理由时才增加依赖。
3. 页面脚本必须先加载 `shared.js`，再加载 `app.js` 或 `profile.js`。
4. 复用现有 CSS 变量、类名语义、`data-action` 事件模式和 `pressable` 反馈，避免为单个元素重复实现一套交互。
5. 使用 `textContent` 或安全的 DOM API 渲染用户输入，不要把档案、问题或历史消息直接拼进 `innerHTML`。
6. 保持中文输入法兼容：发送逻辑必须继续检查 `compositionstart`、`compositionend`、`event.isComposing`。
7. 保持无障碍行为：交互元素使用原生按钮/链接，维护 `aria-*` 状态、键盘焦点、Esc 关闭和 `prefers-reduced-motion`。
8. 保持移动端触摸体验；改动抽屉时同时验证点击、键盘、左侧边缘打开和拖动关闭。
9. 不要提交 `test-output/`、`debug.log`、`.agents/` 或本地临时文件。
10. 不要改写或清理与当前任务无关的工作区改动。

## 状态与持久化

共享状态 API 由 `shared.js` 末尾的 `window.GuoxueApp` 暴露。业务页面应通过该 API 访问数据，不要在页面脚本中复制存储键或绕过标准化逻辑。

当前约束：

- `STORAGE_VERSION = 2`
- `DAILY_LIMIT = 10`
- `HISTORY_LIMIT = 40`
- `CONVERSATION_LIMIT = 90`
- `PROFILE_LIMIT = 20`

修改存储结构时必须：

1. 为读入数据提供容错和标准化。
2. 评估并实现旧版本数据迁移。
3. 保持超限裁剪和无效数据过滤。
4. 保持 `localStorage` 不可用时的内存回退。
5. 验证本地日期跨天后额度刷新，避免使用 UTC 日期替代用户本地日期。

八字档案含敏感个人信息。不得增加日志上报、远程传输或示例真实数据，除非用户明确要求且相关隐私与安全方案已确定。

## 运行与验证

无需安装依赖。使用任意静态 HTTP 服务从仓库根目录运行，例如：

```bash
python -m http.server 8000
```

仓库暂时没有自动化测试或 lint 配置。每次涉及功能或样式的修改后，至少完成与改动相关的手动验证：

- 页面加载无控制台错误，所有运行时图片请求成功。
- 375 px 左右的移动端视口和桌面视口均无意外横向滚动或遮挡。
- 问题发送、思考态、额度扣减、额度耗尽状态正常。
- 中文输入法组合输入不会误触发送。
- 刷新后会话、额度和档案可恢复；历史会话可切换。
- 抽屉的菜单、遮罩、关闭按钮、Esc、焦点循环和手势行为正常。
- 档案新增、编辑、删除及公历/农历日期校验正常。
- 开启“减少动态效果”后不会强制播放非必要动画。

若添加自动化测试，将生成物放入已忽略的 `test-output/`，不要提交截图、录像或调试日志，除非任务明确要求将其作为固定测试资产。

## 部署约定

这是静态站点，所有运行时文件必须显式进入部署产物。新增或重命名 HTML、CSS、JavaScript、字体或资源目录后，同步检查并更新 `.github/workflows/pages.yml` 的 `_site` 复制步骤。

注意：当前工作流的复制列表可能落后于工作区中新加入的档案页面文件；涉及部署或档案功能时，应核对 `profile.html` 和 `profile.js` 是否已包含在产物中。

如果使用 OpenAI Sites，先读取 `.openai/hosting.json`，复用其中的项目 ID，并遵循“精确源码状态 → 保存版本 → 部署该版本”的流程。

## 完成标准

- 改动范围与请求一致，没有覆盖无关的现有修改。
- 页面仍可作为纯静态站点运行。
- 用户可见文案准确区分已实现、模拟和占位能力。
- 相关手动验证已完成，未验证项在交付说明中明确列出。
- 新文件已纳入部署清单或已明确说明为何无需发布。
- README、代码注释和本文件在行为变化后保持同步。
