# AGENTS.md

## 项目概述

构建轻舟发现页demo

## 项目结构

- `src/pages/`：HTML 页面
- `src/js/`：页面脚本和共享状态层
- `src/css/`：共享样式
- `src/components/`：可复用公共组件
- `public/images/`：运行时图片
- `public/icons/`：独立图标资源
- `public/fonts/`：本地字体资源
- `docs/`：产品与接口文档
- `test-output/`：测试生成物，不得提交

## 修改原则

- 修改前先阅读相关代码和现有测试。
- 仅修改完成当前任务所必需的文件。
- 不覆盖或撤销用户已有的修改。
- 优先沿用项目现有架构、命名和实现方式。
- 不随意添加新的依赖。
- 不修改生成文件，除非任务明确要求。
- 涉及配置、数据库结构或公共接口时，同步更新文档和测试。

## 代码规范

- 遵循仓库现有格式化和静态检查配置。
- 保持函数和模块职责单一。
- 新增公共接口时补充类型与必要说明。
- 不提交调试日志、临时文件、密钥或访问令牌。
- 用户可见文字使用简体中文；代码标识符使用英文。

## 测试与验证

完成修改后：

1. 运行与改动直接相关的测试。
2. 运行代码检查和类型检查。
3. 条件允许时运行完整测试套件。
4. 报告执行过的检查及其结果。
5. 无法执行某项检查时说明原因，不要声称已经通过。

## Git 与拉取请求
##
## 分支规则

禁止直接修改 main 分支。

所有开发必须创建新的分支：

格式：

feature/功能名称

例如：

feature/home-layout
feature/chat-ui
feature/payment


## 修改流程

执行任何代码修改前：

1. 查看当前git状态
2. 确认当前分支
3. 如果在main分支，需要创建feature分支


## Commit规范

每完成一个独立功能必须提交commit。

格式：

类型: 内容

例如：

feat: 修改首页顶部布局

fix: 修复按钮间距问题

style: 优化首页UI样式


## 合并规则

禁止自动merge到main。

完成开发后：

1. 提交代码
2. 推送远程分支
3. 等待我确认后合并

## 安全限制

未经用户明确同意，不执行：

- 删除大量文件或不可恢复的操作
- 强制推送
- 重写 Git 历史
- 修改生产环境
- 发布软件包或部署应用
- 向外部服务发送消息或数据

## 子目录规则

如果某个子目录包含自己的 `AGENTS.md`，处理该目录中的文件时优先遵循距离文件最近的规则。
