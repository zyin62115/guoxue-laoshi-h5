# 设计说明

项目采用移动端国风视觉语言，页面结构集中在 `src/pages/`，共享样式位于 `src/css/styles.css`，运行时图片位于 `public/images/`。

当前首页同时承载今日指引、功能入口、问答区和个人中心抽屉；档案表单位于独立页面。后续抽取公共组件时，应保持原生 HTML/CSS/JavaScript、无构建步骤和现有无障碍行为。

各子页面的顶部返回按钮默认调用浏览器历史返回上一页；档案表单的“取消”和微信模拟完成页的返回动作遵循同一规则。每个返回入口仍保留原有站内地址作为无历史记录时的安全兜底。

## 国心解读

国心解读采用“老师陪读式”结构：用户先选择八字档案，由国学老师给出免费总览和八章试读，再选择单章或完整解锁。每章免费试读展示 2—3 行有意义的个性化内容，不使用模糊遮罩或截断句子；已购正文统一包含核心结论、优势天赋、潜在盲区和行动建议。

完整报告售价 ¥88，单章售价 ¥16.8。单章支付金额会抵扣完整报告，购买五章后只保留 ¥4 补全入口。已购章节可进入独立的章节对话页继续追问，对话页显示报告与章节上下文并沿用每日免费额度。

报告章节追问与首页完全分离：从章节进入独立对话页，在该页查看并继续本章问答，返回按钮固定返回原报告章节。首页只承载普通问答，不展示报告上下文或章节消息，个人中心的首页对话记录也不列出报告专属线程。

“限时免费”属于运营展示层，由独立的 promotion 配置和渲染接口管理；对话是否可发送由 quota 业务接口管理。两者不互相读取，因此修改运营文案不会改变额度，未来调整额度策略也不需要改动首页或对话页标签。

报告页面延续首页的暖米白、墨色和朱砂红视觉，复用现有国学老师头像。报告是主内容，老师欢迎语、章节引导和追问卡片承担陪读作用，不引入新的古装人物形象。

用户首次点击“生成我的报告”后，页面先展示报告试读和付费入口，停留 5 秒再展示一次“免费领取完整报告”卡片。从历史报告直接进入时不触发。用户可以关闭并继续原付费流程，也可以点击绿色微信按钮进入本地模拟领取页；两种操作都会记录首访已处理，返回后不重复弹出。正式接入时只替换微信目标，不改变弹层展示规则。

国心解读采用“先选档案，再根据状态查看或生成”的入口规则：首页进入档案选择页并默认勾选上次使用的档案；只有从“我的报告”点击具体报告时才直接打开。没有报告时进入生成流程；档案信息变化时显示“档案已更新”，由用户选择生成新版或查看上一版。档案选择页的按钮分别显示“查看报告”“生成新版报告”或“生成我的报告”。报告按档案快照保留历史版本，删除档案不会删除已生成报告，但已删除档案不能再生成新版。

## 专业排盘入口

专业排盘入口采用四列宫格，完整展示 13 个当前启用项目。视觉延续首页的暖米色、藏青文字、朱橙 / 天青点缀、淡山水和竹枝装饰；每个入口使用独立的国风线性图标，并跳转至对应的独立信息页，不使用底部弹层。

顶部采用单行导航结构：返回键、居中“专业排盘”和右侧“排盘记录”，山水、竹枝与飞鸟仅作为低对比度背景，不再放置副标题或中间图标。导航下方直接展示整图微信咨询 Banner，点击进入本地企业微信模拟页并可返回原排盘页面；接入正式环境时将链接替换为企业微信客服地址。当前实现完成“选择方式 → 填写必要字段 → 提交排盘”的前端流程，不包含历法换算、真实排盘算法、结果页及外部服务跳转。后续按入口逐项接入算法、盘面和记录列表。
